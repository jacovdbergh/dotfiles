[user]
    name = George Boot
    email = 884482+georgeboot@users.noreply.github.com

[init]
	defaultBranch = master

[core]
	excludesfile = ~/.global-gitignore
	editor = code --wait
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
